# reserva-ingresso

re-click > src > main > java > com.re_click:
controller = onde fica as rotas
entity = onde fica as entidades
repository = onde fica as inferfaces jpa
service = as regras de negócio

re-click > src > resources:
templates = onde fica os html
application.properties = onde fica as configurações de banco de dados, porta, etc.
